anthropic>=0.40.0
matplotlib>=3.8.0
flask>=3.0.0
gunicorn>=21.0          # production WSGI server (used by Render / Railway / Fly)
psycopg[binary]>=3.1    # Postgres client; activated when DATABASE_URL is set
